<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9+ blocks plain http by default, and Flutter's dart:io honours that policy, so an
  APK built with API_BASE=http://10.0.2.2:8090/api would install and then never reach the API.
  Cleartext is allowed only to the development hosts below; everything else must be https.
  For a physical phone on your LAN during development, add your machine's address here
  (e.g. <domain>192.168.1.20</domain>) and rebuild. Never add a production host.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false"/>
    <domain-config cleartextTrafficPermitted="true">
        <!-- the emulator's alias for the host machine's loopback -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>

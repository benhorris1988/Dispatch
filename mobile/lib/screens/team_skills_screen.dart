import 'package:flutter/material.dart';

import '../widgets/states.dart';

/// Team & skills — placeholder. Replace the body of [build]; the route in lib/router.dart
/// already points here.
class TeamSkillsScreen extends StatelessWidget {
  const TeamSkillsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const ComingSoon('Team & skills');
  }
}

import 'package:flutter/material.dart';

import '../widgets/states.dart';

/// My week — placeholder. Replace the body of [build]; the route in lib/router.dart
/// already points here.
class MyWeekScreen extends StatelessWidget {
  const MyWeekScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const ComingSoon('My week');
  }
}
